<%= simple_form_for(@line_item) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :product_id %>
    <%= f.input :cart_id %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
